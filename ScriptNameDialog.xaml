<Window x:Class="PinTransferWPF.ScriptNameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Script Name" Height="200" Width="400"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Style="{StaticResource RoundedWindowStyle}">

    <Grid Margin="0,10,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Custom Title Bar -->
        <Rectangle Grid.Row="0" Fill="{StaticResource PrimaryBrush}" MouseDown="TitleBar_MouseDown" Height="30"/>
        <DockPanel Grid.Row="0">
            <TextBlock Text="Script Name" Foreground="White" FontWeight="Bold" VerticalAlignment="Center" Margin="10,0"/>
            <Button Content="✕" Width="30" Height="30" Background="Transparent" Foreground="White" 
                    BorderThickness="0" FontSize="16" Click="CloseButton_Click"
                    DockPanel.Dock="Right" Style="{StaticResource TitleBarButton}"/>
        </DockPanel>

        <!-- Content -->
        <Grid Grid.Row="1" Margin="20" Background="{StaticResource BackgroundBrush}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Enter script name:" Margin="0,0,0,10"/>
            <TextBox Grid.Row="1" x:Name="txtScriptName" Margin="0,0,0,20"/>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="OK" Width="Auto" Margin="0,0,10,0" Click="OkButton_Click" IsDefault="True"/>
                <Button Content="Cancel" Width="Auto" Click="CancelButton_Click" IsCancel="True"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>