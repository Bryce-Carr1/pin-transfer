<Window x:Class="PinTransferWPF.ScriptSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Load Script" Height="400" Width="500"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Style="{StaticResource RoundedWindowStyle}">

    <Grid Margin="0,10,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <!-- Custom Title bar -->
            <RowDefinition Height="*" />
            <!-- Main content -->
        </Grid.RowDefinitions>

        <!-- Custom Title Bar -->
        <Rectangle Grid.Row="0" Fill="{StaticResource PrimaryBrush}" MouseDown="TitleBar_MouseDown" Height="30"/>

        <!-- Title Bar Content -->
        <DockPanel Grid.Row="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" DockPanel.Dock="Left" HorizontalAlignment="Left" Margin="10,0">
                <TextBlock Text="Load Script" Foreground="White" FontWeight="Bold" VerticalAlignment="Center"/>
            </StackPanel>

            <!-- Close Button -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Stretch" DockPanel.Dock="Right" HorizontalAlignment="Right">
                <Button x:Name="CloseButton"
                        Content="✕"
                        Width="30" Height="30"
                        Background="Transparent"
                        Foreground="White"
                        BorderThickness="0"
                        FontSize="16"
                        Click="CloseButton_Click"
                        Style="{StaticResource TitleBarButton}"/>
            </StackPanel>
        </DockPanel>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="10" Background="{StaticResource BackgroundBrush}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Select a script to load:" FontWeight="Bold" Margin="10,10,10,10"/>

            <DataGrid Grid.Row="1" x:Name="ScriptsDataGrid" 
                      AutoGenerateColumns="False" 
                      SelectionMode="Single"
                      IsReadOnly="True"
                      GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Script Name" Binding="{Binding ScriptName}" Width="*"/>
                    <DataGridTextColumn Header="Sources" Binding="{Binding SourceCount}" Width="80"/>
                    <DataGridTextColumn Header="Destinations" Binding="{Binding DestinationCount}" Width="80"/>
                    <DataGridTextColumn Header="Saved Date" Binding="{Binding SavedDate, StringFormat=yyyy-MM-dd HH:mm}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button Content="Load" Width="Auto" Margin="0,0,10,10" Click="LoadButton_Click" IsDefault="True"/>
                <Button Content="Delete" Width="Auto" Margin="0,0,10,10" Click="DeleteButton_Click"/>
                <Button Content="Cancel" Width="Auto" Margin="0,0,10,10" Click="CancelButton_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>